# Frontend (React)

## Requirements
- Node.js 20+

## Run (dev)
1. `npm install`
2. Optional: `copy .env.example .env` and set `VITE_API_BASE_URL`
3. `npm run dev`

App runs on `http://localhost:5173`.
